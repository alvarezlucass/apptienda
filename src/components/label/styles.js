import { Colors } from '../../constants/themes/IndexColors';
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    label: {
        fontSize: 'Play-Regular',
        marginVertical: 5,
        color: Colors.text,
    }
})